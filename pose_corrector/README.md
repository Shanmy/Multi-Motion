# Pose Editing

Code to shift and edit poses. Major examples are shifting intersected poses and shifting vertically misaligned poses to the ground.

## Mesh Overlap

To run a demo that draws a batch from the LAION pose dataloader, finds samples that have high mesh intersection, and separates these meshes, run the command:

```python3 shift_overlapping_poses.py```
